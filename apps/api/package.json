{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "db:migrate": "wrangler d1 migrations apply allshare-db --local",
    "db:seed": "wrangler d1 execute allshare-db --file=./migrations/seed.sql --local",
    "db:studio": "wrangler d1 studio allshare-db --local"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "wrangler": "^4"
  }
}
